# The API's origin when the client is served from somewhere else (leave unset
# when the service serves the client). The service must list this client's
# origin with -allowed-origin.
# VITE_API_BASE=http://127.0.0.1:8765
